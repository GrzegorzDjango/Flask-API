# runtime.txt is for heroku 
# requirements.txt as well (we don't need to install wsgi locally, it will be installed on Heroku)
# uwsgi.ini - heroku specific content
# Procfile - what Dyno we want to use in Heroku
# xx
